"""The explanation layer: retrieved facts in, sourced prose out.

The agent never computes. Every number it may use is assembled here in Python,
carries its source, and is checked against the generated text afterwards.
"""
